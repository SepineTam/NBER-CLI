{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NBER-CLI Configuration",
  "description": "Configuration schema for nber-cli",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "integer",
      "description": "Database schema version"
    },
    "info": {
      "type": "object",
      "description": "Info cache settings",
      "properties": {
        "cache_enabled": {
          "type": "boolean",
          "description": "Whether info cache is enabled"
        },
        "cache_ttl_days": {
          "type": "integer",
          "minimum": 1,
          "description": "Info cache TTL in days"
        }
      }
    },
    "feed": {
      "type": "object",
      "description": "Local database settings",
      "properties": {
        "db-path": {
          "type": "string",
          "description": "Path or sqlite:/// URL for the local database"
        }
      }
    },
    "download": {
      "type": "object",
      "description": "Download settings",
      "properties": {
        "restrict_dir": {
          "type": "boolean",
          "default": true,
          "description": "Restrict downloads to current working directory and its subdirectories"
        },
        "concurrency": {
          "type": "integer",
          "minimum": 1,
          "default": 3,
          "description": "Maximum concurrent downloads"
        }
      }
    },
    "desktop": {
      "type": "object",
      "description": "Desktop app settings",
      "properties": {
        "server_port": {
          "type": "integer",
          "minimum": 1024,
          "description": "Local HTTP sidecar port"
        },
        "feed_refresh_interval_minutes": {
          "type": "integer",
          "minimum": 1,
          "description": "Feed refresh interval in minutes"
        }
      }
    }
  }
}
